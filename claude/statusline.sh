#!/usr/bin/env bash
# Context Lab user-tier statusline. Linked to ~/.claude/statusline.sh.
#
# NOT YET IMPLEMENTED — Phase 1 stub. Populated when the user tier is extracted.
# Prints nothing rather than garbage, so an accidental link is harmless.
exit 0
